# 微信采集黑名单

`sync_chatlog.py` 在每次拉取前读取此文件，命中即跳过：
- 不创建 chatlog 文件
- 不调用 messages API
- 也不参与 sessions 预筛选

## 文件格式

每行一个 wxid。`#` 开头为注释（行内注释也支持：`<wxid>  # 原因`）。空行忽略。大小写敏感。

## 维护方式

直接编辑本文件，新增/删除一行即可。脚本会即时读取。

## 当前黑名单

wxid_nld8b1fqf08y22
